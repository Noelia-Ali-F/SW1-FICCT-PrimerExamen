package com.workflow.system.data.model;

public enum FormFieldType {
  TEXT,
  NUMBER,
  DATE,
  SELECT,
  FILE,
  BOOLEAN,
  TEXTAREA,
  LABEL,
  BUTTON,
  RADIO,
  /** Selector de usuario del sistema (nombre y rol en ejecución). */
  USER
}

